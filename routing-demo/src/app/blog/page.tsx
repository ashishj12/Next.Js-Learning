export default function Blog() {
  return <h1>Blog Page</h1>;
}

// in next js to handle nesting routing we use nested folder structure to handle nested routing

// e.g ->Folder(Blog) - for nested -> use nested (first) (second)
